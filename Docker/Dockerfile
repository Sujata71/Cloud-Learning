# Step 1: Download the official base operating system blueprint
FROM ubuntu

# Step 2: Run a package update inside the image during the build process
RUN apt update

# Step 3: Tell the container to launch the bash shell automatically when it boots up
CMD ["bash"]