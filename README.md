# 🐳 Cloud & DevOps Learning Repository

A hands-on collection of cloud infrastructure and containerization configuration patterns developed from August 7th to August 17th.

## 🛠️ Docker Core Progress
* **Basic Containers Architecture:** Successfully deployed standalone instances of Alpine, Ubuntu, and Nginx.
* **Custom Imaging:** Structured custom recipes utilizing `FROM`, `RUN`, and `CMD` parameters.
* **Networking, Ports, & Volumes:** Engineered external port mapping configurations (`-p 8080:80`) and local volume data bindings (`-v`).
* **Multi-Container Architecture (Docker Compose):** Orchestrated a multi-tier framework linking a custom Python application with a backend MongoDB document store database.
* **Microservices Application Deployment:** Successfully containerized and isolated a multi-file Python Flask web framework project.

## 🚀 Git & GitHub Management Patterns
* Managed parallel feature branches (`main`, `development`, `feature/login`, `feature/python`).
* Successfully resolved localized upstream branch merge conflicts manually.
* Managed open-source repository distributions using cloud `Fork`, `Clone`, `Fetch`, and `Pull Requests`.